export const AZURE_OPENAI_RESOURCE_TYPES: Record<string, string> = {
    'API_KEY': 'API Key',
};

export const DEFAULT_AZURE_OPENAI_API_KEY_CONFIG = {
    requiredEntropy: 4.0,
};

export const DEFAULT_AZURE_OPENAI_URL_CONFIG = {
    requiredEntropy: 0,
};