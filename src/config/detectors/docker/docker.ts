export const DOCKER_RESOURCE_TYPES: Record<string, string> = {
    'REGISTRY': 'Docker Registry Credentials',
};

export const DEFAULT_DOCKER_CONFIG = {
    requiredEntropy: 3.0,
    requiredAuthEntropy: 3.0,
    requiredPasswordEntropy: 1.0,
};