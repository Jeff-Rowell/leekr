import { GroqDetectorConfig } from '../../../types/groq';

export const DEFAULT_GROQ_CONFIG: GroqDetectorConfig = {
    requiredEntropy: 4
};

export const GROQ_RESOURCE_TYPES = {
    'API_KEY': 'Groq API Key'
};