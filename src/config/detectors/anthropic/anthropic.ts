export const ANTHROPIC_RESOURCE_TYPES: Record<string, string> = {
    'ADMIN': 'Admin API Key',
    'USER': 'API Key',
};

export const DEFAULT_ANTHROPIC_API_KEY_CONFIG = {
    requiredEntropy: 0,
};
