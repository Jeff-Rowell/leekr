import { MailchimpDetectorConfig } from '../../../types/mailchimp';

export const DEFAULT_MAILCHIMP_CONFIG: MailchimpDetectorConfig = {
    requiredEntropy: 4.0
};

export const MAILCHIMP_RESOURCE_TYPES = {
    'API_KEY': 'Mailchimp API Key'
};